---
description: Markdown dash lists only (full text in agents/docs/writing-style.md)
alwaysApply: true
---

# List style

Follow `agents/docs/writing-style.md`.

Never use Unicode bullets (`•`, `●`, `◦`). Always write Markdown lists as `- ` then the item.
